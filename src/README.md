# src/ - Deprecated

CUDA 소스는 `implementations/cuda/`로 이동했습니다.

- `cuda_kernel.cu` → `implementations/cuda/single_gemm.cu`
- `tensor_kernel_gemm.cu` → `implementations/cuda/tensor_single_gemm.cu`
- `tensor_kernel_gemm_batched.cu` → `implementations/cuda/batched_gemm.cu`
- `tensor_kernel_gemm_grouped.cu` → `implementations/cuda/grouped_gemm.cu`
- `tensor_kernel_gemm_grouped_half.cu` → `implementations/cuda/grouped_gemm_half.cu`
